[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"

# Enable Next.js runtime for API routes and server-side features
[[plugins]]
  package = "@netlify/plugin-nextjs"
